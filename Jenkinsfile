pipeline{
    agent any
    environment{
        registryCredential = 'dockerhub'
    }
    stages{
        stage('Buid Dockerfile'){
            steps{
               echo 'Test Connection Success'
             }
        }
    }
}
